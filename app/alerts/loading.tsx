import { LoadingSpinner } from "@/components/loading-spinner"

/**
 * Loading component for alerts page
 * Displays spinner while alerts data is being fetched
 */
export default function Loading() {
  return <LoadingSpinner />
}
